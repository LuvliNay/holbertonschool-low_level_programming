#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result.
 * @num1: is the variable given.
 * @num2: is the variable given.
 * Return: SUCCESS (0)
 */

int add(int num1, int num2)
{
	int sum = num1 + num2;

	return (sum);
}
