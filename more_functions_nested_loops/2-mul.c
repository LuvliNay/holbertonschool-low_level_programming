#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: variable given.
 * @b: variable given.
 *
 * Return: Always 0.
 */
int mul(int a, int b);

int mul(int a, int b)
{
	return (a * b);
}
